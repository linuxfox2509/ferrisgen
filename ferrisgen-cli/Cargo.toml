[package]
name = "ferrisgen-cli"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4", features = ["derive"] }
ferrisgen = { path = "../ferrisgen" }
arboard = "2.0"
anyhow = "1.0"

[dev-dependencies]
assert_cmd = "2.0"
predicates = "2.1"
